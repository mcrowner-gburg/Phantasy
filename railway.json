{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:client && npm run copy:client && npm run build:server"
  },
  "deploy": {
    "startCommand": "node server/dist/index.js"
  }
}
