{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:client && npm run copy:client"
  },
  "deploy": {
    "startCommand": "npm run build:server && node server/dist/index.js"
  }
}
