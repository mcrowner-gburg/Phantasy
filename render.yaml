services:
  - type: web
    name: phantasy-app
    env: node
    plan: free
    branch: main
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase: "postgresql://neondb_owner:npg_zYpMJdEB4T1X@ep-bitter-mode-adjkge9f.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require" # if using Render database, otherwise fill manually
      - key: SESSION_SECRET
        value: "supersecretkey12345"
      - key: NODE_ENV
        value: "production"
