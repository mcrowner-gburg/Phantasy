{
  "name": "phantasy-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "cd client && npm install && cd ../server && npm install",
    "build-all": "cd client && npm run build && cd ../server && npm run build:server",
    "start": "cd server && npm start"
  }
}
