{
  "name": "phantasy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix client run build && npm --prefix server run build:server",
    "dev": "concurrently \"npm --prefix client run dev\" \"npm --prefix server run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
