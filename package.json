{
  "name": "phantasy-app",
  "version": "1.0.0",
  "private": true,
   "type": "module",
  "scripts": {
    "dev": "concurrently \"npm:dev:client\" \"npm:dev:server\"",
    "dev:client": "cd client && npm install && vite",
    "dev:server": "cd server && npm install && tsx index.ts",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm install && vite build",
    "build:server": "cd server && npm install && esbuild index.ts --bundle --platform=node --format=esm --outdir=dist --external:@neondatabase/serverless --external:ws --external:twilio --external:zod --external:drizzle-zod --external:drizzle-orm --external:express --external:express-session --external:@sendgrid/mail --external:connect-pg-simple --external:nanoid --external:bcrypt",
    "start": "node server/dist/index.js",
    "check": "tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.21.10",
    "tsx": "^3.12.7",
    "vite": "^5.4.19",
    "@vitejs/plugin-react": "^4.0.0"
  }
  
}
