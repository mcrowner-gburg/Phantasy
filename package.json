{
  "name": "phantasy-server",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build:server": "esbuild index.ts --bundle --platform=node --format=esm --outdir=dist --external:@neondatabase/serverless --external:ws --external:twilio --external:zod --external:drizzle-zod --external:drizzle-orm --external:express --external:express-session --external:@sendgrid/mail --external:connect-pg-simple --external:nanoid --external:bcrypt",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "bcrypt": "^5.1.0",
    "connect-pg-simple": "^8.0.0",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "nanoid": "^4.0.0",
    "twilio": "^4.14.0",
    "zod": "^3.23.2",
    "drizzle-orm": "^0.23.0",
    "drizzle-zod": "^0.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.5",
    "@types/express": "^4.17.19",
    "@types/express-session": "^1.17.6",
    "@types/node": "^20.5.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
