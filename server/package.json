{
  "name": "phantasy-server",
  "private": true,
  "scripts": {
    "dev": "tsx index.ts",
    "build": "build": "esbuild index.tsx --loader:.tsx=ts --platform=node --bundle --format=esm --outdir=dist",
    "start": "NODE_ENV=production node dist/index.js",
    "check": "tsc"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "tsx": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
