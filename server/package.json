{
  "name": "phantasy-server",
  "private": true,
  "scripts": {
    "dev": "tsx index.ts",
    "build": "esbuild index.ts --platform=node --bundle --format=esm --outdir=dist",
    "start": "node dist/index.js",
    "check:duplicates": "node -e \"let s=require('fs').readFileSync('package.json','utf8'); let keys=[]; JSON.parse(s,(k,v)=>{if(k){if(keys.includes(k)){throw new Error('Duplicate key: '+k)}; keys.push(k)} return v}); console.log('No duplicate keys found!')\"",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "express": "^4.18.2",
    "bcrypt": "^5.1.0",
    "nanoid": "^4.0.0",
    "drizzle-orm": "^0.28.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.4",
    "tsx": "^3.12.7",
    "typescript": "^5.2.2"
  }
}
