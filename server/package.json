{
  "name": "phantasy-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev index.ts",
    "build:server": "esbuild index.ts --platform=node --bundle --format=esm --outdir=dist",
    "start": "node dist/index.js",
    "check": "tsc"
  },
  "dependencies": {
    "express": "^4.18.2",
    "bcrypt": "^5.1.0",
    "drizzle-orm": "^0.24.0",
    "nanoid": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.18.18",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.0"
  }
}
