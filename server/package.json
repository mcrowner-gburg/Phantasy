{
  "name": "phantasy-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx index.ts",
    "build": "esbuild index.ts --bundle --platform=node --format=esm --outdir=dist --external:@neondatabase/serverless --external:ws --external:twilio --external:zod --external:drizzle-zod --external:drizzle-orm --external:express --external:express-session --external:@sendgrid/mail --external:connect-pg-simple --external:nanoid --external:bcrypt",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "@neondatabase/serverless": "^0.1.13",
    "ws": "^8.13.0",
    "twilio": "^4.23.0",
    "zod": "^3.22.0",
    "drizzle-zod": "^0.2.0",
    "bcrypt": "^5.1.1",
    "drizzle-orm": "^0.21.0",
    "nanoid": "^4.0.0",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "connect-pg-simple": "^8.0.0"
    "esbuild": "^0.20.15"
  },
  "devDependencies": {
    "tsx": "^3.12.7",
    "esbuild": "^0.20.15"
  }
}
