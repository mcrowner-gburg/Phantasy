{
  "name": "rest-express",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check:duplicates": "node -e \"let s=require('fs').readFileSync('package.json','utf8'); let keys=[]; JSON.parse(s,(k,v)=>{if(k){if(keys.includes(k)){throw new Error('Duplicate key: '+k)}; keys.push(k)} return v}); console.log('No duplicate keys found!')\"",

    "build": "npm run check:duplicates && esbuild index.tsx --loader:.tsx=tsx --platform=node --bundle --format=esm --outdir=dist",

    "dev": "tsx index.tsx",

    "start": "NODE_ENV=production node dist/index.js",

    "check": "tsc",

    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "tsx": "^3.12.7",
    "esbuild": "^0.19.3",
    "typescript": "^5.3.0"
  }
}
